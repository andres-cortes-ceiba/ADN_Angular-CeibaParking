import { FilterParkingLotByTypePipe } from './filter-parking-lot-by-type.pipe';

describe('FilterParkingLotByTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterParkingLotByTypePipe();
    expect(pipe).toBeTruthy();
  });
});
