import { FilterVehicleByTypePipe } from './filter-vehicle-by-type.pipe';

describe('FilterVehicleByTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterVehicleByTypePipe();
    expect(pipe).toBeTruthy();
  });
});
